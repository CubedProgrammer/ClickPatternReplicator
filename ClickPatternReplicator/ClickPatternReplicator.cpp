#include <iostream>
#include <vector>
#include "RecordPattern.h"
int main(int argl, char *argv[])
{
	std::string cmd;
	std::vector<std::pair<unsigned, unsigned>>pattern;
	std::cin >> cmd;
	while (cmd != "exit")
	{
		if (cmd == "record")
		{
		}
		std::cin >> cmd;
	}
	return 0;
}